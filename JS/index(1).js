//Accept an integer value and a message from user and print the message that many number of times.
let number = parseInt(prompt("Enter number: "));
let str = prompt("Enter a string");
for( let i =0 ;i<number; i++){
  console.log(str);
}